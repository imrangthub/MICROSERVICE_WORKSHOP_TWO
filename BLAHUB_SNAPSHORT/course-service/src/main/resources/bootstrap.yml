server:
  port: ${PORT:8082}
spring:
  application:
    name: course-service
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9092/eureka/
management:
  endpoints:
    web:
      exposure:
        include: "*"